# locals {
#   project_name         = "JumpReact"
#   environment          = "prod"
#   resource_prefix      = "${local.project_name}-${local.environment}"
#   api_gateway_name     = "${local.resource_prefix}-api"
#   s3_bucket_name       = "${local.resource_prefix}-bucket"
#   lambda_function_name = "${local.resource_prefix}-lambda"
# }
